name: Daily Cricket Update (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: daily-cricket-update-disabled
  cancel-in-progress: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Stop disabled writer
        run: |
          echo "GitHub Daily Cricket Update is intentionally disabled."
          echo "Use Codex app automations only after manual re-approval."
          exit 1
